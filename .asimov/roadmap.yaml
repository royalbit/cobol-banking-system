# Asimov Protocol Roadmap
#
# COBOL Banking System → Java Spring Boot Migration
#
# See: README.md for project overview
# See: CHANGELOG.md for release history
#
# RULE: Commit and push after every version is completed

current:
  version: "0.4.0"
  status: released
  summary: "REST API endpoints"
  goal: "API_READY"
  deliverables:
    - "[x] Spring Boot REST controllers"
    - "[x] Account endpoints (CRUD, deposit, withdraw)"
    - "[x] Transaction endpoints (history, mini-statement)"
    - "[x] OpenAPI/Swagger documentation"
    - "[x] Unit tests (80% coverage minimum)"
    - "[x] Integration tests (API endpoint testing)"
    - "[x] 96 tests passing"

completed:
  - version: "0.1.0"
    summary: "Domain models & test fixtures"
    deliverables:
      - "[x] Java project scaffold (Spring Boot 3.x, Java 17)"
      - "[x] Domain models: Account, Transaction"
      - "[x] Test fixtures from CUSTOMERS.DAT / TRANSACTIONS.DAT"
      - "[x] Equivalence test harness"

  - version: "0.2.0"
    summary: "Core banking operations"
    deliverables:
      - "[x] Account CRUD (create, view, soft delete)"
      - "[x] Deposit/Withdrawal with validation"
      - "[x] Interest application (2% for savings)"
      - "[x] 60 equivalence tests passing"

  - version: "0.3.0"
    summary: "Transaction logging & statements"
    deliverables:
      - "[x] Transaction audit trail"
      - "[x] Mini statements (last 5 transactions)"
      - "[x] Date/time formatting (COBOL style)"
      - "[x] 74 equivalence tests passing"

next:

  - version: "0.5.0"
    summary: "Modern Web UI (Vue 3 + Tailwind)"
    goal: "MODERNIZATION"
    deliverables:
      - "Vue 3 + Vite + Tailwind CSS"
      - "Dashboard with account overview"
      - "Transaction history with filtering"
      - "Deposit/Withdraw forms"
      - "Mini statement view"
      - "Component tests (Vitest)"
      - "E2E tests (Playwright)"

  - version: "1.0.0"
    summary: "Production-ready release"
    goal: "PRODUCTION"
    deliverables:
      - "Docker containerization"
      - "Documentation & deployment guide"
      - "Demo data seeding"

backlog:
  - "Authentication & authorization (JWT)"
  - "Multi-currency support"
  - "Statement export (PDF/CSV)"
  - "Dark mode toggle"
