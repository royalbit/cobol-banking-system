# Asimov Protocol - Sprint Tracking
# https://github.com/royalbit/asimov-protocol

sprint:
  current: "v0.4.0 - REST API endpoints"
  started: "2025-12-01"
  status: done

  tasks:
    - "[x] Add Spring Boot Web & Validation dependencies"
    - "[x] Add OpenAPI/Swagger (springdoc-openapi)"
    - "[x] Create DTOs (CreateAccountRequest, TransactionRequest, etc.)"
    - "[x] AccountController - CRUD, deposit, withdraw, apply-interest"
    - "[x] TransactionController - history, mini-statement"
    - "[x] Global exception handler"
    - "[x] Integration tests (AccountControllerTest, TransactionControllerTest)"
    - "[x] JaCoCo coverage (80% minimum)"
    - "[x] All 96 tests passing"

  blockers: []

  notes: |
    Milestone v0.4.0 complete. REST API endpoints implemented:

    Endpoints:
    - GET    /api/accounts              - List all accounts
    - GET    /api/accounts/{id}         - Get account by ID
    - POST   /api/accounts              - Create account
    - POST   /api/accounts/{id}/deposit - Deposit money
    - POST   /api/accounts/{id}/withdraw - Withdraw money
    - DELETE /api/accounts/{id}         - Soft delete account
    - POST   /api/accounts/apply-interest - Apply interest
    - GET    /api/accounts/{id}/transactions - Transaction history
    - GET    /api/accounts/{id}/transactions/mini-statement - Last 5

    Swagger UI: http://localhost:8080/swagger-ui.html

    Test coverage:
    - 22 new controller integration tests
    - Total: 96 tests passing (was 74)
    - JaCoCo 80% coverage verified

    Ready for v0.5.0: Vue 3 + Tailwind UI.
