# Asimov Protocol - Sprint Tracking
# https://github.com/royalbit/asimov-protocol

sprint:
  current: "v0.1.0 - Domain models & test fixtures"
  started: "2025-12-01"
  status: completed

  tasks:
    - "[x] Analyze COBOL data structures (BANKACCT.cob)"
    - "[x] Parse .DAT file formats (CUSTOMERS.DAT, TRANSACTIONS.DAT)"
    - "[x] Create Java project scaffold (Spring Boot 3.2, Java 17)"
    - "[x] Create domain models (Account, Transaction)"
    - "[x] Create enums (AccountType, AccountStatus, TransactionType)"
    - "[x] Create JPA repositories"
    - "[x] Create test fixtures from COBOL data"
    - "[x] Build equivalence test harness"
    - "[x] All 33 tests passing"

  blockers: []

  notes: |
    Milestone v0.1.0 complete. Java project structure:

    java-banking/
    ├── src/main/java/com/royalbit/banking/
    │   ├── BankingApplication.java
    │   ├── domain/
    │   │   ├── Account.java
    │   │   ├── AccountStatus.java
    │   │   ├── AccountType.java
    │   │   ├── Transaction.java
    │   │   └── TransactionType.java
    │   └── repository/
    │       ├── AccountRepository.java
    │       └── TransactionRepository.java
    └── src/test/java/com/royalbit/banking/
        ├── TestFixtures.java
        ├── EquivalenceTest.java
        └── domain/
            ├── AccountTest.java
            └── TransactionTest.java

    COBOL → Java mapping verified:
    - ACCT-ID PIC X(10) → String accountId
    - NAME PIC X(30) → String customerName
    - BALANCE PIC 9(7)V99 → BigDecimal balance
    - ACCT-TYPE PIC X(1) → AccountType enum (S/C)
    - STATUS → AccountStatus enum (A/I)
    - TRANS-TYPE PIC X(1) → TransactionType enum (D/W/I/X)

    Ready for v0.2.0: Core banking operations.
